---
const { color, font, transform, extra } = Astro.props;

const classes = [
  font,
  "font-semibold",
  "text-5xl",
  color,
  transform ?? "normal-case",
  extra ? [...extra] : "",
];
---

<h2 class:list={classes}>
  <slot />
</h2>
